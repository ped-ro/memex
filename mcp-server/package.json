{
  "name": "vault-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Obsidian vault — hybrid semantic + full-text search via pgvector",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:http": "MCP_TRANSPORT=http node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.5.0",
    "pg": "^8.13.0",
    "express": "^4.18.2"
  }
}
